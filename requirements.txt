# pymerlin itself has NO required runtime dependencies (pyproject.toml declares
# `dependencies = []`); local `simulate()` is pure Python. The packages below are
# the optional feature extras plus the dev/docs toolchain, so a venv built from
# this file can run the tutorials, guides, and tests.

# Optional feature extras (pyproject.toml [project.optional-dependencies])
bokeh>=3.4.2      # plotting
numpy>=2.0.0      # plotting
spiceypy>=6.0.0   # spice

# Needed by demo/model.py, which parses its spacecraft parameters from TOML. Listed here so
# `demo/main.py` and the local simulate() engine work; the WORKER gets it a different way --
# `pymerlin package` detects the import and writes it into the model JAR's requirements.txt.
# This file is never consulted for that, and is not bundled into any JAR.
toml>=0.10.2

# Dev / docs toolchain
myst-parser
build
twine
sphinx
pytest
furo
